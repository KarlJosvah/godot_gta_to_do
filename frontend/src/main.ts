import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')

// const bcrypt = require('bcrypt');
// bcrypt.hash('your_new_password_here', 10).then(console.log);